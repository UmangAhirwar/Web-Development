// Write a function which takes a string and return count of each character in the string.

function charCount(str){
    let currIndex = 0;
    for(let i = 0; i < str.length; i++){
        currIndex = i;
        if(str[currIndex] == str[i]){
            console.log(str[i], count)
        }
    }
}