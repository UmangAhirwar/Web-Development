<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show users</title>
    <style>
        table {
            width: 90%;
            margin: 50px auto;
            border-collapse: separate;
            border-spacing: 0;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
        }
    
        th, td {
            padding: 16px 24px;
            text-align: left;
        }
    
        th {
            background-color: #2c3e50;
            color: #fff;
            font-size: 18px;
            letter-spacing: 1px;
        }
    
        tr {
            transition: all 0.3s ease-in-out;
        }
    
        td {
            border-bottom: 1px solid #ecf0f1;
            color: #34495e;
            font-size: 16px;
        }
    
        tr:hover {
            background-color: #f0f8ff;
            transform: scale(1.01);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        }
    
        tr:last-child td {
            border-bottom: none;
        }
    
        /* Optional: Add subtle gradient background */
        body {
            background: linear-gradient(to right, #dfe9f3, #ffffff);
        }
        .premium-btn {
        background: #2c3e50;
        color: #ffffff;
        padding: 12px 24px;
        font-size: 16px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .premium-btn:hover {
        background: #1abc9c;
        color: #fff;
        transform: scale(1.05);
        box-shadow: 0 6px 20px rgba(26, 188, 156, 0.3);
    }

    .premium-btn:active {
        transform: scale(0.98);
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
    }
    </style>
    
</head>
<body>
    <form action="/user/new">
        <button class="premium-btn">Add user</button>
    </form>
    <table>
        <tr>
            <th>ID</th>
            <th>USERNAME</th>
            <th>EMAIL</th>
            <th>EDIT</th>
            <th>DELETE</th>
        </tr>
        <%for(user of users){ %>
            <tr>
                <td><%= user.id%></td>
                <td><%= user.username%></td>
                <td><%= user.email%></td>
                <td>
                    <form method="GET" action="user/<%= user.id %>/edit">
                        <button class="premium-btn">Edit user</button>
                    </form>
                </td>
                <td>
                    <form method="POST" action="user/<%= user.id %>?_method=DELETE">
                        <button class="premium-btn">Delete</button>
                    </form>
                </td>
            </tr>
        <%}%>
    </table>
</body>
</html>

